import { Client } from './client';
export declare class Filter {
    client: Client;
    constructor(client: Client);
    getOperator(op: string | null, op2: string | null): string;
    joinOperator(op: string | null): string;
    escapeValue(value: string, field: object | null): string;
    formatValues(values: string | null, lookup: string, op: string | null, field: object | null): string;
    parseDMQL(value: string, fields: object[]): string;
    transform(table: string, filter: string): Promise<string>;
}
