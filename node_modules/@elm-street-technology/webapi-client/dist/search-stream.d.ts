/// <reference types="node" />
import { Readable } from 'stream';
import { Client } from './client';
import { IRetsOptions, Search } from './search';
export declare class SearchStream {
    client: Client;
    search: Search;
    constructor(client: Client, search: Search);
    isPaused(stream: Readable): Promise<void>;
    finish(stream: Readable, type: string, payload: any): void;
    query(id: string, className: string, query: string, config?: IRetsOptions): {
        retsStream: Readable;
    };
}
