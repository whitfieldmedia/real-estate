export default class RetsReplyError extends Error {
    replyCode: number;
    replyText: string;
    retsMethod: string;
    replyTag: string;
    originalError?: Error;
    constructor(retsContext: {
        retsMethod: string;
    }, replyCode: number, replyText: string, originalError?: Error);
}
